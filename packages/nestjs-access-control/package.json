{
  "name": "nestjs-access-control",
  "version": "0.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "@nestjs/common": "^7.0.7",
    "@nestjs/core": "^7.0.7",
    "accesscontrol": "^2.2.1"
  },
  "devDependencies": {
    "gts": "^2.0.0",
    "typescript": "^3.8.3",
    "@types/node": "^10.0.3"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run check"
  }
}
