import { ReferenceIdInterface } from '@concepta/ts-core';

/**
 * interface for the assignable entity, the entity otp will have
 * relationship with
 */
export interface OtpAssigneeInterface extends ReferenceIdInterface {}
