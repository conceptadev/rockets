# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
coverage-e2e
coverage-rockets-*
*.lcov

# Generated reports
CODE_SIMPLIFICATION_REPORT.md
graphify-out/
code-analysis-out/
# Per-run analyser output (index/symbols dumps) — regenerate, never commit
**/docs/code-analysis/runs/
junit.xml

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# TypeScript v1 declaration files
typings/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test

# parcel-bundler cache (https://parceljs.org/)
.cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and *not* Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# JetBrainz
.idea
.DS_Store

# generated docs
/docs

# generated SDK (orval)
**/generated/api
**/generated/models
**/generated/zod

# .yarn meta
.yarn/*
!.yarn/versions/
!.yarn/versions/*.yml

# local agent scratchpad — never commit
.context/


# Claude Code personal overrides (not committed)
.claude/settings.local.json
.claude/CLAUDE.local.md

#Ignore cursor AI rules
.cursor/rules/codacy.mdc

# Codacy CLI's resolved pattern set — generated, not authored. Two
# 196KB byte-identical files carrying `source: auto` and a null
# repositoryId, i.e. the CLI's own default rather than a decision this
# repo made. The decision that IS ours is the tracked
# `.codacy/codacy.yaml` (runtimes + tool versions); these regenerate
# from it. `.codacy/.gitignore` cannot hold this — it ignores itself
# and is never committed, so it only works on one machine.
.codacy/codacy.config.json
.codacy/codacy.config.baseline.json

# Editor / OS / backup leftovers — never commit these
*.bak
*.orig
*~
.DS_Store
Thumbs.db
